<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="280dp"
    android:padding="5dp">

    <ImageView
        android:id="@+id/iv_main_item_profile"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@mipmap/ic_launcher"/>

    <TextView
        android:id="@+id/tv_main_item_user"
        android:layout_width="180dp"
        android:layout_height="40dp"
        app:layout_constraintStart_toEndOf="@id/iv_main_item_profile"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="5dp"/>

    <ImageView
        android:id="@+id/iv_main_item_image"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        app:layout_constraintTop_toBottomOf="@id/iv_main_item_profile"
        android:layout_marginTop="5dp"
        android:src="@color/colorAccent"/>

    <Button
        android:layout_width="40dp"
        android:layout_height="40dp"
        app:layout_constraintTop_toBottomOf="@id/iv_main_item_image"
        app:layout_constraintStart_toStartOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>